{
  "name": "chat-application",
  "version": "1.0.0",
  "engines": {
    "node": ">=18.0.0"
  },
  "description": "Simple chat application",
  "main": "backend/server.js",
  "scripts": {
    "start": "node backend/server.js",
    "dev": "nodemon backend/server.js",
    "build": "mkdir -p dist && cp -r frontend/* dist/ && echo 'Build completed'",
    "deploy": "wrangler publish"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.18.2",
    "marked": "^15.0.2",
    "node-fetch": "^3.3.2",
    "@cloudflare/kv-asset-handler": "^0.3.0",
    "wrangler": "^3.0.0"
  },
  "devDependencies": {
    "nodemon": "^3.1.7"
  }
}